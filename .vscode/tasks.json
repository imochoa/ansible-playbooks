{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "local",
            "type": "shell",
            "command": "just run-local",
            "options": {
                "cwd": "${workspaceFolder}",
                // "env": {
                //     "ANSIBLE_CONFIG": "${workspaceFolder}/ansible.cfg"
                // }
            },
            "group": {
                // "kind": "test",
                "isDefault": true
            },
            "presentation": {
                "reveal": "always",
                "echo": true,
                // often need to write a password
                "focus": true,
                "panel": "dedicated"
            },
        }
    ]
}